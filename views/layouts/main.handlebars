<html>
  <head>
    <meta charset="UTF-8" />
    <title>ToDo App</title>

    <style>
      .errors { color: red; font-weight: bold; margin-bottom: 1em; }
    </style>
  </head>
  <body>

    {{#if error}}
      <div class="errors">{{error}}</div>
    {{/if}}

    {{{body}}}

  </body>
</html>
